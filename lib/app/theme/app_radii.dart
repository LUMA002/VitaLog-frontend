import 'package:flutter/material.dart';

abstract final class AppRadii {
  static const r = Radius.circular(16);
  static const card = Radius.circular(20);
  static const pill = Radius.circular(9999);
}
